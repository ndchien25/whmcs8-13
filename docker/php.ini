; ===========================================
; PHP Configuration for WHMCS 9.0
; ===========================================

; Error handling
display_errors = On
display_startup_errors = On
error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT
log_errors = On

; File uploads
upload_max_filesize = 100M
post_max_size = 100M
max_file_uploads = 20

; Memory & execution
memory_limit = 256M
max_execution_time = 300
max_input_time = 300
max_input_vars = 5000

; Session
session.gc_maxlifetime = 1440

; Timezone (adjust as needed)
date.timezone = Asia/Ho_Chi_Minh

; Allow URL fopen (required by WHMCS)
allow_url_fopen = On

; Disable dangerous functions (optional security hardening)
; disable_functions = exec,passthru,shell_exec,system,proc_open,popen

; Opcache settings for performance
opcache.enable = 1
opcache.memory_consumption = 256
opcache.interned_strings_buffer = 32
opcache.max_accelerated_files = 30000
opcache.revalidate_freq = 60
opcache.validate_timestamps = 0

; WHMCS loads many PHP files per request. Larger realpath cache reduces
; repeated filesystem checks, especially on Docker Desktop bind mounts.
realpath_cache_size = 16M
realpath_cache_ttl = 600
